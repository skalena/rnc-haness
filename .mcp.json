{
  "mcpServers": {
    "rnc": {
      "type": "sse",
      "url": "${user_config.base_url}/sse",
      "headers": {
        "Authorization": "Bearer ${user_config.token}"
      },
      "note": "RNC MCP server — read-only, tenant-scoped tools for zooming into a legacy module (getModuleRules, getModuleDataModel, getUirModule, getSourceFile). The token comes from the plugin's own configuration, so it lives in secure storage rather than in this file, in settings.json or in the conversation. Reconfigure with /plugin."
    }
  }
}
