{
  "mcpServers": {
    "rnc": {
      "command": "npx",
      "args": ["-y", "@skalena/rnc", "mcp", "proxy"],
      "note": "RNC MCP server — read-only, tenant-scoped tools for zooming into a legacy module (getModuleRules, getModuleDataModel, getUirModule). Authentication comes from the credential store written by `rnc mcp login`, so no token lives in this file or in your environment. Override the host with RNC_BASE_URL."
    }
  }
}
